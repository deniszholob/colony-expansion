import { MenuNewGameComponent } from './menu-new-game.component';

describe('MenuNewGameComponent', () => {
  let component: MenuNewGameComponent;

  beforeEach(() => {
    component = new MenuNewGameComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
