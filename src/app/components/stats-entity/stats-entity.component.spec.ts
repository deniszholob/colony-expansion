import { StatsEntityComponent } from './stats-entity.component';

describe('StatsEntityComponent', () => {
  let component: StatsEntityComponent;

  beforeEach(() => {
    component = new StatsEntityComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
