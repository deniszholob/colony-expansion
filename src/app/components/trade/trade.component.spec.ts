import { TradeComponent } from './trade.component';

describe('TradeComponent', () => {
  let component: TradeComponent;

  beforeEach(() => {
    component = new TradeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
