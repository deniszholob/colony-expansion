import { StatsBarComponent } from './stats-bar.component';

describe('StatsBarComponent', () => {
  let component: StatsBarComponent;

  beforeEach(() => {
    component = new StatsBarComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
