import { StatsGroupComponent } from './stats-group.component';

describe('StatsGroupComponent', () => {
  let component: StatsGroupComponent;

  beforeEach(() => {
    component = new StatsGroupComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
