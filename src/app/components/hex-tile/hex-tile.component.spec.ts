import { HexTileComponent } from './hex-tile.component';

describe('HexTileComponent', () => {
  let component: HexTileComponent;

  beforeEach(() => {
    component = new HexTileComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
