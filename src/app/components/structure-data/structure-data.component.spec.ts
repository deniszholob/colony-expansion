import { StructureDataComponent } from './structure-data.component';

describe('StructureDataComponent', () => {
  let component: StructureDataComponent;

  beforeEach(() => {
    component = new StructureDataComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
